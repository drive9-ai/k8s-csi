apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# This is a fail-closed base. Select an image through a checked overlay.
resources:
  - namespace.yaml
  - csidriver.yaml
  - rbac.yaml
  - controller.yaml
  - node.yaml
  - storageclass.yaml
  - volumeattributesclass.yaml
  - storageclass-rwx.yaml
  - volumeattributesclass-rwx.yaml
